"""Workshop Feedback Portal — backend package (POC).

A single Python AWS Lambda function, path-routed behind an API Gateway HTTP API,
implementing the behavior defined in docs/api-contract.md and docs/data-model.md.
"""
